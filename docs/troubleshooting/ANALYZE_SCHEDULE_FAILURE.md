# 🔍 스케줄 실패 원인 분석

## 📋 로그 분석 결과

### 현재 상황
- **현재 시간 (UTC):** `2025-11-20T12:05:00.504Z`
- **현재 시간 (KST):** `2025. 11. 21. 오전 06:05:00`
- **시간 범위:** `2025-11-20T11:55:00.504Z` ~ `2025-11-20T12:10:00.504Z` (UTC)
  - 과거 10분 ~ 미래 5분

### 문제점

**로그 메시지:**
```
[execute-schedules] No schedules to execute in the time window
```

**확인된 스케줄:**
- 모두 미래 날짜 (2025-11-26 이후)
- 시간 범위 내에 실행할 스케줄이 없음

---

## 🔍 가능한 원인

### 원인 1: 스케줄 시간이 범위 밖에 있음

**8:40, 42, 45분 스케줄이:**
- 이미 과거 10분 이전에 있었을 수 있음
- 또는 아직 미래 시간 범위 밖에 있을 수 있음

**현재 시간 범위:**
- 과거 10분: `2025-11-20T11:55:00` (UTC) = `2025-11-21 06:55` (KST)
- 미래 5분: `2025-11-20T12:10:00` (UTC) = `2025-11-21 07:10` (KST)

**8:40, 42, 45분 스케줄 (KST):**
- `2025-11-21 08:40` (KST) = `2025-11-20 23:40` (UTC)
- `2025-11-21 08:42` (KST) = `2025-11-20 23:42` (UTC)
- `2025-11-21 08:45` (KST) = `2025-11-20 23:45` (UTC)

**문제:** 이 시간들은 현재 시간 범위 밖에 있음!

### 원인 2: 스케줄 상태가 "scheduled"가 아님

**스케줄이 이미 실행되었거나 실패했을 수 있음:**
- `status = "sent"` (이미 실행됨)
- `status = "failed"` (실패함)

### 원인 3: 스케줄이 데이터베이스에 없음

**스케줄이 생성되지 않았을 수 있음**

---

## 🔧 해결 방법

### 방법 1: 시간 범위 확대 (권장)

**현재 설정:**
- 과거 10분 ~ 미래 5분

**권장 설정:**
- 과거 30분 ~ 미래 30분

**이유:**
- 놓친 스케줄을 처리할 수 있음
- 미래 스케줄도 미리 준비할 수 있음

### 방법 2: 데이터베이스에서 스케줄 확인

**SQL 쿼리:**
```sql
-- 8:40, 42, 45분 스케줄 확인 (KST 기준)
SELECT 
  id,
  schedule_name,
  scheduled_time,
  status,
  fail_reason,
  target_channel
FROM tts_schedule_requests
WHERE scheduled_time >= '2025-11-20 23:30:00'
  AND scheduled_time <= '2025-11-21 00:00:00'
ORDER BY scheduled_time;
```

**확인 사항:**
- 스케줄이 존재하는지
- `status`가 "scheduled"인지
- `scheduled_time`이 올바른지

### 방법 3: 스케줄 재생성

**8:40, 42, 45분 스케줄이 없다면:**
- 새로 스케줄 생성
- 시간 설정 확인 (KST 기준)

---

## 📝 다음 단계

1. **데이터베이스 확인** - 스케줄이 실제로 있는지 확인
2. **시간 범위 확대** - 놓친 스케줄 처리
3. **스케줄 상태 확인** - "scheduled" 상태인지 확인

**SQL 쿼리를 실행하여 스케줄 상태를 확인해 주세요!** 🔍

