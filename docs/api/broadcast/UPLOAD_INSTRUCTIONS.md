# 📤 Hostinger 서버에 파일 업로드 가이드

## 🚨 현재 상황

API 파일이 로컬에는 있지만 **Hostinger 서버에 업로드되지 않아서** 404 에러가 발생하고 있습니다.

## 📁 업로드할 파일

로컬 경로: `/Users/june/Documents/GitHub/voicecraft-designer/api/broadcast/`

업로드할 파일 (PHP 버전):
1. ✅ `index.php` - 메인 API 파일
2. ✅ `test.php` - 테스트 파일  
3. ✅ `.htaccess` - Apache 설정 파일

## 🎯 업로드 경로 (매우 중요!)

### ❌ 잘못된 경로:
```
public_html/api/broadcast/           ← 메인 도메인 (틀림!)
```

### ✅ 올바른 경로:
```
public_html/tts/api/broadcast/       ← 서브도메인 (맞음!)
```

**이유**: `tts.nanum.online`은 서브도메인이므로 `public_html/tts/`가 루트입니다.

---

## 📝 업로드 방법

### 방법 1: Hostinger File Manager 사용 (권장)

1. **Hostinger 대시보드 접속**
   - https://www.hostinger.com 로그인

2. **File Manager 열기**
   - 대시보드 → **File Manager** 클릭

3. **폴더 구조 확인 및 생성**
   
   **`public_html` 폴더로 이동**
   ```
   public_html/
   ```
   
   **`tts` 폴더 확인**
   - `tts` 폴더가 있으면 클릭하여 이동
   - `tts` 폴더가 없으면:
     - 우클릭 → **New Folder**
     - 이름: `tts`
     - 생성 후 `tts` 폴더로 이동
   
   **`api` 폴더 생성**
   - `tts` 폴더 안에서:
     - 우클릭 → **New Folder**
     - 이름: `api`
     - 생성 후 `api` 폴더로 이동
   
   **`broadcast` 폴더 생성**
   - `api` 폴더 안에서:
     - 우클릭 → **New Folder**
     - 이름: `broadcast`
     - 생성 후 `broadcast` 폴더로 이동

4. **파일 업로드**
   
   **현재 위치 확인**: `public_html/tts/api/broadcast/`
   
   **파일 업로드**:
   - **Upload Files** 버튼 클릭
   - 또는 드래그 앤 드롭
   - 다음 3개 파일 선택:
     - `index.php`
     - `test.php`
     - `.htaccess`
   
   **⚠️ 주의**:
   - `.htaccess` 파일은 숨김 파일입니다
   - 파일 선택 시 "모든 파일 표시" 옵션 활성화
   - 또는 직접 입력: `.htaccess`

5. **파일 권한 확인**
   
   업로드한 파일들을 선택:
   - 우클릭 → **Change Permissions**
   - 파일: **644**
   - 폴더: **755**

### 방법 2: FTP 사용

1. **FTP 클라이언트 연결**
   - FileZilla, WinSCP 등 사용
   - Hostinger에서 FTP 정보 확인:
     - 호스트: `ftp.hostinger.com` (또는 제공된 FTP 호스트)
     - 사용자명: Hostinger 계정 정보
     - 비밀번호: Hostinger 계정 비밀번호

2. **서버 경로로 이동**
   ```
   /public_html/tts/api/broadcast/
   ```
   
   폴더가 없으면 생성:
   ```
   /public_html/tts/
   /public_html/tts/api/
   /public_html/tts/api/broadcast/
   ```

3. **파일 업로드**
   - 로컬: `api/broadcast/` 폴더
   - 서버: `/public_html/tts/api/broadcast/` 폴더
   - 다음 파일 업로드:
     - `index.php`
     - `test.php`
     - `.htaccess`

4. **권한 설정**
   - 파일: 644
   - 폴더: 755

---

## ✅ 업로드 확인

### 1. Hostinger File Manager에서 확인

다음 구조가 있어야 합니다:
```
public_html/
└── tts/
    └── api/
        └── broadcast/
            ├── index.php     ← 있음
            ├── test.php      ← 있음
            └── .htaccess     ← 있음
```

### 2. 브라우저에서 테스트

**테스트 1: test.php 직접 접근**
```
https://tts.nanum.online/api/broadcast/test.php
```

**예상 결과**: JSON 응답 표시
```json
{
  "status": "ok",
  "message": "All checks passed! API endpoint is ready to use.",
  ...
}
```

**실제 결과가 HTML이면**: 파일이 업로드되지 않았거나 경로가 잘못됨

### 3. 터미널에서 테스트

```bash
# test.php 테스트
curl https://tts.nanum.online/api/broadcast/test.php

# 메인 API 테스트 (POST)
curl -X POST https://tts.nanum.online/api/broadcast \
  -H "Content-Type: audio/mpeg" \
  -d "test audio data"
```

**예상 결과**: JSON 응답

---

## 🔍 문제 해결

### 문제 1: 파일을 업로드했는데 여전히 HTML이 반환됨

**확인 사항**:
1. 파일이 올바른 경로에 있는지 확인: `public_html/tts/api/broadcast/`
2. 파일 이름이 정확한지 확인: `index.php`, `test.php`, `.htaccess`
3. 파일 권한이 644인지 확인
4. 브라우저 캐시 삭제 후 다시 시도

### 문제 2: 404 에러 계속 발생

**확인 사항**:
1. 파일 경로 확인: `public_html/tts/api/broadcast/`
2. 서브도메인 설정 확인: Hostinger → 도메인 → 서브도메인
3. `.htaccess` 파일이 올바른 위치에 있는지 확인

### 문제 3: PHP 파일이 다운로드됨

**원인**: PHP가 실행되지 않음

**해결**:
- Hostinger에서 PHP 버전 확인
- PHP가 활성화되어 있는지 확인 (일반적으로 기본 활성화)

---

## 📋 최종 체크리스트

업로드 전:
- [ ] 로컬 `api/broadcast/` 폴더에 `index.php`, `test.php`, `.htaccess` 파일 확인

업로드 중:
- [ ] Hostinger File Manager 또는 FTP로 연결
- [ ] `public_html/tts/api/broadcast/` 폴더로 이동 (또는 생성)
- [ ] 3개 파일 업로드
- [ ] 파일 권한 644 설정

업로드 후:
- [ ] File Manager에서 파일 위치 확인
- [ ] `https://tts.nanum.online/api/broadcast/test.php` 브라우저에서 접근
- [ ] JSON 응답이 나오는지 확인
- [ ] `curl` 명령어로 테스트

---

**파일을 업로드한 후 다시 테스트해보세요!** 🚀

업로드 후에도 문제가 지속되면:
1. 업로드한 파일의 실제 경로를 확인해주세요
2. 브라우저에서 `test.php` 접근 시 어떤 응답이 나오는지 알려주세요

